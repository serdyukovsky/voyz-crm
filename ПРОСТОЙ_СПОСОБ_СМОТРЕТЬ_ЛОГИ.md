# 🔥 ПРОСТОЙ СПОСОБ СМОТРЕТЬ ЛОГИ

## ❌ Проблема: команды не работают

Если команды с `grep` не работают, есть более простые способы.

---

## ✅ СПОСОБ 1: Просто tail (БЕЗ grep)

**В терминале бэкенда:**

```bash
tail -f /tmp/backend-full.log
```

Или если не работает:

```bash
tail -f /tmp/backend-run.log
```

**Что делать:**
1. Запустите эту команду
2. В ДРУГОМ терминале или в браузере запустите импорт
3. Смотрите все логи - они будут прокручиваться в реальном времени

---

## ✅ СПОСОБ 2: Запустить бэкенд ПРЯМО в терминале (лучший способ!)

**Остановите текущий бэкенд:**
```bash
pkill -f "nest start"
pkill -f "node.*main"
```

**Запустите бэкенд ПРЯМО в терминале:**
```bash
cd /workspaces/voyz-crm/voyz-crm/crm-backend
npm run start:dev
```

**Тогда:**
- Все логи будут ПРЯМО в этом терминале
- Не нужны никакие файлы
- Видно все сразу

---

## ✅ СПОСОБ 3: Посмотреть что УЖЕ есть в логах

Если импорт УЖЕ прошел, просто посмотрите что было:

```bash
# Посмотреть последние 100 строк
tail -100 /tmp/backend-full.log

# Или поискать конкретное
grep "MAP DEAL ROW RESULT" /tmp/backend-full.log
```

---

## 🎯 РЕКОМЕНДАЦИЯ

**Лучший способ для отладки:**

1. Остановите бэкенд в фоне
2. Запустите ПРЯМО в терминале: `cd crm-backend && npm run start:dev`
3. Все логи будут прямо перед глазами



