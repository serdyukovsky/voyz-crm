# 🔍 Как проверить логи backend

## ⚡ Быстрый старт

### 1. Проверить, что backend работает
```bash
curl http://localhost:3001/api/health
```
Должен вернуть: `{"status":"ok",...}`

### 2. Посмотреть последние логи
```bash
# Основной лог (watch mode)
tail -50 /tmp/backend.log

# Runtime лог (если запущен через nohup)
tail -50 /tmp/backend_runtime.log
```

### 3. Мониторинг в реальном времени
Откройте **новый терминал** и выполните:
```bash
tail -f /tmp/backend.log | grep --line-buffered -E '🔥|import|deals|error' -i
```

Затем в браузере выполните dry-run импорта - вы увидите логи в реальном времени.

## 🌐 Проверка в браузере (Network Tab)

### Шаги:
1. Откройте DevTools: `F12` или `Ctrl+Shift+I`
2. Перейдите на вкладку **Network**
3. Установите фильтр: **XHR** или **Fetch**
4. Выполните dry-run импорта в приложении
5. Найдите запрос: `POST /api/import/deals?dryRun=true`

### Что проверить:

#### ✅ Request Headers:
- `Content-Type: application/json`
- `Authorization: Bearer ...`

#### ✅ Request Payload (тело запроса):
Должно быть:
```json
{
  "rows": [
    { "Column1": "value1", "Column2": "value2" },
    ...
  ],
  "mapping": {
    "title": "Column1",
    "stageId": "Column2"
  },
  "pipelineId": "some-pipeline-id",
  "workspaceId": "some-workspace-id"
}
```

❌ **НЕ должно быть:**
- `file`
- `delimiter`

#### ✅ Response:
- Status: `200 OK` (даже при ошибках в dry-run)
- Body: JSON с полями:
  - `summary: { total, created, updated, failed, skipped }`
  - `errors: []`
  - `globalErrors: []`

## 🔍 Что искать в логах

### ✅ Успешный запрос:
```
🔥 CONTROLLER ENTRY - importDeals endpoint called
🔥 DTO received: { hasRows: true, rowsCount: 5, ... }
🔥 IMPORT ENTRY - importDeals called
[IMPORT PIPELINE DEBUG] { pipelineId: '...', pipelineLoaded: true, ... }
```

### ❌ Проблема: запрос идет не на тот endpoint
```
🔥 CONTROLLER ENTRY - importContacts endpoint called (NOT deals!)
```
**Решение:** Проверьте URL в Network tab - должен быть `/api/import/deals`

### ❌ Проблема: нет данных
```
🔥 DTO received: { hasRows: false, rowsCount: 0, ... }
```
**Решение:** Проверьте, что CSV файл загружен и распарсен на фронтенде

### ❌ Проблема: ошибка "CSV file is required"
Если видите эту ошибку, но в логах нет `🔥 CONTROLLER ENTRY - importDeals`, значит:
- Запрос не доходит до контроллера
- Возможно, проблема в ValidationPipe или middleware

## 📋 Полезные команды

### Запустить скрипт проверки
```bash
./check-backend-logs.sh
```

### Найти все ошибки
```bash
grep -i "error\|exception\|failed" /tmp/backend.log | tail -20
```

### Найти все запросы к импорту
```bash
grep "importDeals\|importContacts" /tmp/backend.log | tail -20
```

### Очистить логи
```bash
> /tmp/backend.log
```

### Перезапустить backend с логами
```bash
cd /workspaces/voyz-crm/voyz-crm/crm-backend
pkill -f "nest start"
npm run start:dev > /tmp/backend.log 2>&1 &
```

## 🎯 Пошаговая диагностика

### Шаг 1: Проверьте, что backend работает
```bash
curl http://localhost:3001/api/health
```

### Шаг 2: Откройте мониторинг логов
В **новом терминале**:
```bash
tail -f /tmp/backend.log | grep --line-buffered -E '🔥|import|deals|error' -i
```

### Шаг 3: Выполните dry-run в браузере
- Загрузите CSV
- Нажмите "Run Dry Run"

### Шаг 4: Проверьте логи
В терминале должны появиться:
- `🔥 CONTROLLER ENTRY - importDeals endpoint called`
- `🔥 DTO received: ...`
- `🔥 IMPORT ENTRY - importDeals called`

### Шаг 5: Проверьте Network tab
- URL правильный?
- Content-Type правильный?
- Payload содержит `rows`, а не `file`?

## 📝 Файлы с документацией

- `HOW_TO_CHECK_LOGS.md` - подробная документация
- `QUICK_CHECK.md` - быстрая справка
- `check-backend-logs.sh` - скрипт автоматической проверки






