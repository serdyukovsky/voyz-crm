# Как проверить логи импорта сделок

## 📍 Где находятся логи

Логи бэкенда могут выводиться в:
1. **Консоль/терминал** - где запущен бэкенд
2. **Файл логов** - обычно `/tmp/backend-run.log` (если запущен через скрипт)

## 🔍 Ключевые логи для проверки

### 1. Лог маппинга CSV строки → данные сделки

**Лог:** `[MAP DEAL ROW RESULT] Row {rowNumber}:`

**Что проверять:**
```javascript
{
  number: "...",           // ✅ Есть ли номер сделки?
  title: "...",            // ✅ Есть ли название?
  amount: 1000,            // ❓ Есть ли amount? (может быть null)
  budget: 500,             // ❓ Есть ли budget? (может быть null)
  assignedToId: "...",     // ❓ Есть ли assignedToId? (может быть null)
  contactId: "...",        // ❓ Есть ли contactId? (может быть null)
  companyId: "...",        // ❓ Есть ли companyId? (может быть null)
  expectedCloseAt: "...",  // ❓ Есть ли дата? (может быть null)
  description: "...",      // ❓ Есть ли описание? (может быть null)
  tags: [...],             // ❓ Есть ли теги? (может быть undefined)
  rejectionReasons: [...], // ❓ Есть ли причины отказа? (может быть undefined)
  stageId: "...",          // ✅ Есть ли stageId?
}
```

**Как проверить:**
```bash
# В файле логов
grep "MAP DEAL ROW RESULT" /tmp/backend-run.log | tail -20

# Или следить в реальном времени
tail -f /tmp/backend-run.log | grep "MAP DEAL ROW RESULT"
```

---

### 2. Лог данных сделки перед созданием (actual import)

**Лог:** `[IMPORT DEAL DATA] Row {rowNumber} deal data:`

**Что проверять:**
```javascript
{
  number: "...",
  title: "...",
  amount: 1000,            // ❓ Сохранился ли amount?
  budget: 500,             // ❓ Сохранился ли budget?
  assignedToId: "...",     // ❓ Сохранился ли assignedToId?
  contactId: "...",        // ❓ Сохранился ли contactId?
  companyId: "...",        // ❓ Сохранился ли companyId?
  expectedCloseAt: "...",  // ❓ Сохранилась ли дата?
  description: "...",      // ❓ Сохранилось ли описание?
  tags: [...],             // ❓ Сохранились ли теги?
  rejectionReasons: [...], // ❓ Сохранились ли причины отказа?
  reason: "...",           // ❓ Сохранилась ли причина?
}
```

**Как проверить:**
```bash
grep "IMPORT DEAL DATA" /tmp/backend-run.log | tail -20
```

---

### 3. Лог данных перед batchCreateDeals

**Лог:** `[BATCH CREATE DEAL DATA] Row {index + 1}:`

**Что проверять:**
```javascript
{
  number: "...",
  title: "...",
  amount: 1000,            // ❓ Правильно ли преобразован в число?
  budget: 500,             // ❓ Правильно ли преобразован в число?
  description: "...",      // ❓ Есть ли описание?
  tags: [...],             // ❓ Есть ли теги?
  rejectionReasons: [...], // ❓ Есть ли причины отказа?
  reason: "...",           // ❓ Есть ли причина?
  assignedToId: "...",     // ❓ Есть ли assignedToId?
  contactId: "...",        // ❓ Есть ли contactId?
  companyId: "...",        // ❓ Есть ли companyId?
  expectedCloseAt: "...",  // ❓ Правильно ли преобразована дата?
  isUpdate: false,         // Создание или обновление?
}
```

**Как проверить:**
```bash
grep "BATCH CREATE DEAL DATA" /tmp/backend-run.log | tail -20
```

---

### 4. Лог маппинга (что пришло с фронтенда)

**Лог:** `[IMPORT MAPPING]` или `[IMPORT REQUEST]`

**Что проверять:**
- Какие поля сопоставлены в mapping
- Есть ли поля amount, budget, description, rejectionReasons в mapping

**Как проверить:**
```bash
grep -A 20 "IMPORT REQUEST\|IMPORT MAPPING" /tmp/backend-run.log | tail -50
```

---

## 🔄 Процесс проверки пошагово

### Шаг 1: Проверить маппинг

```bash
grep -A 30 "IMPORT REQUEST" /tmp/backend-run.log | grep -E "mapping|bodyKeys" -A 20
```

**Ожидаемый результат:**
- В `mapping` должны быть поля: `amount`, `budget`, `description`, `rejectionReasons` (если они сопоставлены)

---

### Шаг 2: Проверить извлечение данных из CSV

```bash
grep "MAP DEAL ROW RESULT" /tmp/backend-run.log | tail -1 | jq '.'
```

**Ожидаемый результат:**
- Если поле сопоставлено и есть в CSV → значение должно быть в логе
- Если поле не сопоставлено → должно быть `undefined` или `null`

**Пример проблемы:**
```javascript
// ❌ Плохо - поле сопоставлено, но значение null
amount: null,  // Хотя в CSV было значение

// ✅ Хорошо - поле сопоставлено и значение есть
amount: "1000",

// ✅ Нормально - поле не сопоставлено
amount: undefined,
```

---

### Шаг 3: Проверить передачу данных в dealsWithNumber

```bash
grep "IMPORT DEAL DATA" /tmp/backend-run.log | tail -1 | jq '.'
```

**Ожидаемый результат:**
- Значения должны совпадать с `MAP DEAL ROW RESULT`
- Если в `MAP DEAL ROW RESULT` было значение, но здесь `null` → проблема в передаче

---

### Шаг 4: Проверить данные перед сохранением в БД

```bash
grep "BATCH CREATE DEAL DATA" /tmp/backend-run.log | tail -1 | jq '.'
```

**Ожидаемый результат:**
- Для `amount`: должно быть число (не строка, не null)
- Для `budget`: должно быть число или null
- Для `rejectionReasons`: должен быть массив строк (не undefined, не null)

**Пример проблемы:**
```javascript
// ❌ Плохо - массив пустой, хотя должно быть значение
rejectionReasons: [],

// ✅ Хорошо - массив с значениями
rejectionReasons: ["price", "competitor"],

// ❌ Плохо - undefined вместо массива
rejectionReasons: undefined,
```

---

## 🐛 Типичные проблемы и их признаки

### Проблема 1: Поле не сопоставлено

**Признак:**
```javascript
// В MAP DEAL ROW RESULT:
amount: undefined,  // Или отсутствует поле
```

**Решение:** Проверить маппинг на фронтенде

---

### Проблема 2: Значение в CSV пустое

**Признак:**
```javascript
// В MAP DEAL ROW RESULT:
amount: null,  // Поле сопоставлено, но значение null
```

**Решение:** Проверить CSV файл - есть ли значения в этой колонке

---

### Проблема 3: Данные не передаются в dealsWithNumber

**Признак:**
```javascript
// В MAP DEAL ROW RESULT:
amount: "1000",

// В IMPORT DEAL DATA:
amount: null,  // ❌ Значение потерялось
```

**Решение:** Проверить код создания `dealToCreate` - возможно поле не включено

---

### Проблема 4: Неправильное преобразование типов

**Признак:**
```javascript
// В BATCH CREATE DEAL DATA:
amount: "1000",  // ❌ Строка вместо числа
budget: null,    // ❌ null хотя в CSV было значение
```

**Решение:** Проверить логику преобразования в `batchCreateDeals`

---

### Проблема 5: Данные не сохраняются в БД

**Признак:**
```javascript
// В BATCH CREATE DEAL DATA:
rejectionReasons: ["price", "competitor"],  // ✅ Есть данные

// Но в БД после импорта:
rejectionReasons: []  // ❌ Массив пустой
```

**Решение:** Проверить Prisma запрос - возможно поле не включено в `data`

---

## 📝 Скрипт для автоматической проверки

Создайте файл `check-import-logs.sh`:

```bash
#!/bin/bash

LOG_FILE="/tmp/backend-run.log"

echo "=== 1. Маппинг (последний запрос) ==="
grep -A 30 "IMPORT REQUEST" $LOG_FILE | tail -30 | grep -E "mapping|bodyKeys" -A 15

echo -e "\n=== 2. Извлечение данных (последняя строка) ==="
grep "MAP DEAL ROW RESULT" $LOG_FILE | tail -1

echo -e "\n=== 3. Передача данных (последняя строка) ==="
grep "IMPORT DEAL DATA" $LOG_FILE | tail -1

echo -e "\n=== 4. Данные перед сохранением (последняя строка) ==="
grep "BATCH CREATE DEAL DATA" $LOG_FILE | tail -1

echo -e "\n=== 5. Ошибки импорта ==="
grep -i "error\|failed\|exception" $LOG_FILE | tail -10
```

Запустить:
```bash
chmod +x check-import-logs.sh
./check-import-logs.sh
```

---

## 💡 Советы

1. **Используйте `tail -f` для мониторинга в реальном времени:**
   ```bash
   tail -f /tmp/backend-run.log | grep -E "MAP DEAL ROW RESULT|IMPORT DEAL DATA|BATCH CREATE DEAL DATA"
   ```

2. **Фильтруйте по номеру строки:**
   ```bash
   grep "Row 5" /tmp/backend-run.log | grep -E "MAP DEAL ROW RESULT|IMPORT DEAL DATA|BATCH CREATE DEAL DATA"
   ```

3. **Сохраняйте логи для анализа:**
   ```bash
   tail -1000 /tmp/backend-run.log > import-debug.log
   ```

4. **Используйте `jq` для форматирования JSON:**
   ```bash
   grep "MAP DEAL ROW RESULT" /tmp/backend-run.log | tail -1 | sed 's/.*: //' | jq '.'
   ```
