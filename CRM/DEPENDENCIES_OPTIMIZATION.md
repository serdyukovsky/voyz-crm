# Оптимизация зависимостей package.json

## ✅ Выполненные изменения

### 1. Удалены неиспользуемые зависимости

#### ❌ `@vercel/analytics` (удален)
- **Причина:** Закомментирован в App.tsx, используется только в app/layout.tsx (Next.js, не используется в Vite)
- **Размер:** ~50KB
- **Эффект:** Уменьшение bundle size

#### ❌ `react-is` (удален)
- **Причина:** Не используется нигде в коде
- **Размер:** ~5KB
- **Эффект:** Уменьшение bundle size

### 2. Зафиксированы версии "latest"

#### ✅ `date-fns: "latest"` → `"^4.1.0"`
- **Причина:** "latest" - плохая практика, может привести к breaking changes
- **Эффект:** Предсказуемость сборок, стабильность

#### ✅ `recharts: "latest"` → `"^2.15.0"`
- **Причина:** "latest" - плохая практика, может привести к breaking changes
- **Эффект:** Предсказуемость сборок, стабильность

### 3. Перемещены в devDependencies

#### ✅ `autoprefixer` (перемещен)
- **Было:** dependencies
- **Стало:** devDependencies
- **Причина:** Используется только при сборке, не нужен в production bundle

#### ✅ `@tanstack/react-query-devtools` (перемещен)
- **Было:** dependencies
- **Стало:** devDependencies
- **Причина:** Dev-инструмент, не нужен в production bundle
- **Размер:** ~100KB
- **Эффект:** Уменьшение production bundle

## 📊 Итоговая статистика

### Удалено из dependencies:
- `@vercel/analytics` (~50KB)
- `react-is` (~5KB)
- `autoprefixer` (перемещен)
- `@tanstack/react-query-devtools` (~100KB, перемещен)

### Исправлено:
- `date-fns`: "latest" → "^4.1.0"
- `recharts`: "latest" → "^2.15.0"

### Ожидаемое уменьшение production bundle:
- **~155KB** (удаленные + перемещенные зависимости)

## 📝 Diff изменений

### Удалено из dependencies:
```diff
- "@tanstack/react-query-devtools": "^5.91.1",
- "@vercel/analytics": "1.3.1",
- "autoprefixer": "^10.4.20",
- "react-is": "^19.2.0",
```

### Изменено в dependencies:
```diff
- "date-fns": "latest",
+ "date-fns": "^4.1.0",

- "recharts": "latest",
+ "recharts": "^2.15.0",
```

### Добавлено в devDependencies:
```diff
+ "@tanstack/react-query-devtools": "^5.91.1",
+ "autoprefixer": "^10.4.20",
```

## ✅ Проверка работоспособности

После изменений нужно выполнить:

```bash
# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановить зависимости
npm install

# Проверить сборку
npm run build

# Запустить dev сервер
npm run dev
```

## 🎯 Результаты

✅ Удалены неиспользуемые зависимости  
✅ Зафиксированы версии для стабильности  
✅ Правильное разделение dependencies/devDependencies  
✅ Уменьшение production bundle на ~155KB  

## 📋 Оставшиеся зависимости (все используются)

- `embla-carousel-react` - используется в carousel.tsx
- `input-otp` - используется в input-otp.tsx
- `vaul` - используется в drawer.tsx
- `cmdk` - используется в 8 файлах
- `react-resizable-panels` - используется в resizable.tsx
- `react-day-picker` - используется в 11 файлах
- Все `@radix-ui/*` пакеты - используются в UI компонентах
