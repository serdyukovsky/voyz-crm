# Анализ зависимостей package.json

## 🔍 Найденные проблемы

### 1. ❌ Неиспользуемые зависимости
- `@vercel/analytics` - закомментирован в App.tsx, используется только в app/layout.tsx (Next.js, не используется в Vite)
- `react-is` - не используется нигде в коде

### 2. ⚠️ Неправильные версии
- `date-fns: "latest"` - плохая практика, нужно зафиксировать версию
- `recharts: "latest"` - плохая практика, нужно зафиксировать версию

### 3. 📦 Неправильное размещение
- `autoprefixer` - должен быть в devDependencies, а не в dependencies
- `@tanstack/react-query-devtools` - должен быть в devDependencies, а не в dependencies

### 4. ✅ Используемые зависимости (оставить)
- `embla-carousel-react` - используется в components/ui/carousel.tsx
- `input-otp` - используется в components/ui/input-otp.tsx
- `vaul` - используется в components/ui/drawer.tsx
- `cmdk` - используется в 8 файлах
- `react-resizable-panels` - используется в components/ui/resizable.tsx
- `react-day-picker` - используется в 11 файлах

## 📊 Размер зависимостей

### Тяжелые зависимости:
- `recharts` - ~200KB (но уже lazy loaded)
- `@radix-ui/*` - много пакетов, но все используются
- `socket.io-client` - ~100KB (нужен для real-time)

## ✅ Рекомендации

1. Удалить `@vercel/analytics` и `react-is`
2. Зафиксировать версии `date-fns` и `recharts`
3. Переместить `autoprefixer` и `@tanstack/react-query-devtools` в devDependencies
