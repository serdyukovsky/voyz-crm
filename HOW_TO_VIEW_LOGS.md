# 🔍 Как просматривать логи бэкенда

## Проблема: команда `tail -f` ничего не показывает

Это происходит потому что:
1. **Логи пишутся только при выполнении операций** (например, при импорте)
2. **Нужно запустить импорт**, чтобы появились новые записи в логе
3. **Возможно, логи пишутся в другой файл**

## ✅ Правильный способ просмотра логов

### Вариант 1: Просмотр в реальном времени (рекомендуется)

**В терминале где запущен бэкенд**, выполните:

```bash
# Смотрим все логи в реальном времени
tail -f /tmp/backend-full.log
```

**Или фильтруем только нужное:**

```bash
# Только логи импорта
tail -f /tmp/backend-full.log | grep --line-buffered -i "import\|MAP\|DEAL\|BATCH"
```

**Что делать:**
1. Запустите эту команду в одном терминале
2. В другом терминале или в браузере запустите импорт
3. Вы увидите логи в реальном времени

---

### Вариант 2: Просмотр последних логов

Если импорт уже прошел, посмотрите последние записи:

```bash
# Последние 100 строк с логами импорта
grep -i "import\|MAP\|DEAL\|BATCH" /tmp/backend-full.log | tail -50
```

---

### Вариант 3: Поиск в файле логов

```bash
# Найти все логи импорта
grep "MAP DEAL ROW RESULT" /tmp/backend-full.log

# Найти последний импорт
grep "IMPORT REQUEST\|🔥 IMPORT REQUEST" /tmp/backend-full.log | tail -1 -A 100
```

---

## 📂 Где находятся логи

В вашем случае логи могут быть в:
- `/tmp/backend-full.log` - основные логи (если запущен через `npm run start:dev`)
- `/tmp/backend-run.log` - логи production режима (если запущен `node dist/main.js`)
- `/tmp/backend.log` - логи из скрипта запуска

**Проверьте какой файл активнее:**

```bash
# Посмотреть какой файл больше (больше логов)
ls -lh /tmp/backend*.log

# Посмотреть последнее изменение
ls -lt /tmp/backend*.log | head -3
```

---

## 🎯 Быстрая проверка наличия логов

```bash
# Есть ли вообще логи импорта?
grep -c "importDeals" /tmp/backend-full.log
grep -c "importDeals" /tmp/backend-run.log

# Где больше логов?
echo "backend-full.log: $(grep -c . /tmp/backend-full.log 2>/dev/null || echo 0) строк"
echo "backend-run.log: $(grep -c . /tmp/backend-run.log 2>/dev/null || echo 0) строк"
```

---

## 🔄 Что делать если логов нет

1. **Убедитесь что импорт запущен:**
   - Откройте браузер
   - Загрузите CSV файл
   - Запустите импорт

2. **Проверьте что бэкенд запущен:**
   ```bash
   ps aux | grep "nest start\|node.*main" | grep -v grep
   ```

3. **Проверьте доступность бэкенда:**
   ```bash
   curl http://localhost:3001/api/health
   ```

4. **Перезапустите бэкенд с выводом логов:**
   ```bash
   cd /workspaces/voyz-crm/voyz-crm/crm-backend
   npm run start:dev
   ```
   (Это запустит в foreground режиме - логи будут прямо в терминале)

---

## 💡 Правильная последовательность действий

1. **Откройте два терминала:**
   - Терминал 1: `tail -f /tmp/backend-full.log | grep --line-buffered -i "import\|MAP\|DEAL"`
   - Терминал 2: (для выполнения импорта)

2. **В браузере запустите импорт**

3. **Смотрите логи в терминале 1**

---

## 🐛 Если все равно ничего не видно

Возможно логи не пишутся. Проверьте:

```bash
# Проверить права на файл
ls -la /tmp/backend*.log

# Проверить что процесс пишет в файл
lsof | grep backend.*log

# Попробовать другой файл
tail -f /tmp/backend-run.log
tail -f /tmp/backend.log
```



