# Анализ: contactMethods при импорте сделок

## 🔍 Проблема

**Маппинг отправляется:**
```javascript
allMappedFields: ['title', 'stageId', 'contactMethods', 'number']
inverted: {
  title: 'Название сделки',
  stageId: 'Этап сделки',
  contactMethods: 'Способ связи',  // ⚠️ Это поле контактов!
  number: 'ID'
}
```

## ❌ Почему contactMethods не работает

### 1. contactMethods - это поле КОНТАКТОВ, не сделок

**В коде:**
- `ContactFieldMapping` содержит `contactMethods?: string`
- `DealFieldMapping` НЕ содержит `contactMethods`

### 2. В mapDealRow нет обработки contactMethods

Функция `mapDealRow` обрабатывает только поля из `DealFieldMapping`:
- `number`, `title`, `amount`, `budget`
- `stageId`, `assignedToId`, `contactId`, `companyId`
- `expectedCloseAt`, `description`, `tags`
- `rejectionReasons`, `reason`

**НО НЕ:** `contactMethods`, `directions`, `link`, `subscriberCount` и другие поля контактов

### 3. Результат

Когда бэкенд получает маппинг с `contactMethods`:
- Поле игнорируется в `mapDealRow` (его нет в `DealFieldMapping`)
- Значение не извлекается из CSV
- Поле не попадает в результат импорта

## ✅ Доступные поля для импорта сделок

Из `DealsImportMetaDto` и `DealFieldMapping`:

### Обязательные:
- `title` ✅ (уже маппится)

### Основные поля:
- `number` ✅ (уже маппится)
- `stageId` ✅ (уже маппится)
- `amount` ❌ (не маппится)
- `budget` ❌ (не маппится)

### Поля контакта (для связи сделки с контактом):
- `email` - для резолва contactId по email
- `phone` - для резолва contactId по phone
- `contactId` - прямой ID контакта
- `companyId` - ID компании

### Дополнительные поля:
- `description` ❌ (не маппится)
- `tags` ❌ (не маппится)
- `expectedCloseAt` ❌ (не маппится)
- `assignedToId` / `ownerId` ❌ (не маппится)
- `rejectionReasons` ❌ (не маппится)
- `reason` ❌ (не маппится)

## 🎯 Вывод

**Маппинг отправляется, НО:**

1. ✅ `title`, `number`, `stageId` - работают (это поля сделок)
2. ❌ `contactMethods` - НЕ работает (это поле контактов, не сделок)

**Для импорта сделок нужно маппить поля из списка доступных для deals, а не для contacts.**

## 📝 Рекомендация

Если нужно импортировать "способ связи" для сделки, то:
1. Это поле должно быть кастомным полем сделки (customField)
2. ИЛИ это поле контакта, и нужно сначала создать/найти контакт, а потом связать со сделкой через `contactId`

Текущий маппинг `contactMethods` для deals не будет работать, потому что это поле контактов.



