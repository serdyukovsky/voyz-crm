# 🔍 Быстрая проверка логов

## Проблема: `tail -f | grep` ничего не показывает

**Причина:** `tail -f` показывает только **новые** строки, которые появляются после запуска команды. Если импорт не запущен, новых строк нет.

## ✅ Решения

### Решение 1: Посмотреть последние логи (если импорт уже прошел)

```bash
# Посмотреть последние логи импорта
grep "MAP DEAL ROW RESULT\|IMPORT DEAL DATA\|BATCH CREATE DEAL DATA" /tmp/backend-run.log | tail -20
```

Или используйте скрипт:
```bash
./watch-import-logs.sh
```

---

### Решение 2: Мониторинг с правильным буферированием

Используйте `--line-buffered`:

```bash
tail -f /tmp/backend-full.log | grep --line-buffered -i "import\|MAP\|DEAL\|BATCH"
```

**Важно:** Запустите импорт в браузере, чтобы увидеть новые логи!

---

### Решение 3: Смотреть все логи без фильтра

Если хотите видеть все логи:

```bash
tail -f /tmp/backend-full.log
```

Затем запустите импорт, и вы увидите все логи в реальном времени.

---

### Решение 4: Использовать скрипт

```bash
./watch-import-logs.sh
```

Этот скрипт:
1. Покажет последние логи импорта
2. Будет ждать новых логов
3. Правильно обрабатывает буферизацию

---

## 🎯 Правильная последовательность

1. **Запустите мониторинг:**
   ```bash
   ./watch-import-logs.sh
   ```
   ИЛИ
   ```bash
   tail -f /tmp/backend-full.log | grep --line-buffered -i "MAP\|IMPORT\|BATCH"
   ```

2. **Запустите импорт в браузере**

3. **Смотрите логи в терминале**

---

## 📝 Проверка существующих логов

Если импорт уже прошел, посмотрите что было:

```bash
# Найти все записи импорта
grep "importDeals" /tmp/backend-run.log | wc -l

# Посмотреть последний импорт
grep "🔥 IMPORT REQUEST" /tmp/backend-run.log | tail -1 -A 50

# Посмотреть последние данные сделки
grep "MAP DEAL ROW RESULT" /tmp/backend-run.log | tail -1
```



