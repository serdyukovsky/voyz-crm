# Детальный анализ проблем импорта сделок

## 🔍 Проблема 1: Поля контакта не передаются

### Анализ кода:

**Текущая реализация:**
```typescript
// В цикле обработки validRows:
for (let rowIndex = 0; rowIndex < validRows.length; rowIndex++) {
  const row = validRows[rowIndex]; // Это элемент из validRows
  
  // Пытаемся найти processedRow:
  const processedRow = processedRows.find(r => r.title === row.title && r.stageId === row.stageId);
  const processedRowIndex = processedRows.indexOf(processedRow);
  
  // Используем processedRowIndex для доступа к rows:
  const csvRow = rows[processedRowIndex];
}
```

### Проблемы:

1. **validRows создается из updatedRows:**
   ```typescript
   const validRows = updatedRows.filter(row => {
     // фильтрация по stageId/stageValue
   });
   ```
   - `validRows` - это ПОДМНОЖЕСТВО `updatedRows`
   - Индекс в `validRows` НЕ равен индексу в `updatedRows` или `processedRows`

2. **updatedRows создается из processedRows:**
   - `updatedRows` = `processedRows` с добавленным `stageId` (если было `stageValue`)
   - Соответствие индексов сохраняется

3. **processedRows создается из rows:**
   - `processedRows` создается последовательно из `rows` через `mapDealRow`
   - Соответствие индексов сохраняется (processedRows[i] соответствует rows[i])

4. **НО:** После фильтрации `validRows = updatedRows.filter(...)` индексы теряются!

### Правильное решение:

Нужно сохранять индекс исходной CSV строки (`rows`) во время создания `processedRows`:
```typescript
// При создании processedRows сохранять исходный индекс:
processedRows.push({
  ...dealData,
  originalRowIndex: rowIndex, // Сохранить индекс исходной строки
});

// Потом использовать:
const csvRow = rows[processedRow.originalRowIndex];
```

ИЛИ сохранять данные контактов сразу при обработке строк в `processedRows`, а не позже.

---

## 🔍 Проблема 2: assignedToId/ownerId не работает правильно

### Текущая логика в mapDealRow:

```typescript
if (defaultAssignedToId && !ownerField) {
  // Если указан дефолтный ответственный и нет маппинга, используем его для всех строк
  assignedToId = defaultAssignedToId;
} else if (ownerField) {
  const ownerValue = getValue(ownerField);
  if (ownerValue) {
    // Пытаемся найти пользователя
    // ...
    if (foundUserId) {
      assignedToId = foundUserId;
    } else {
      // Owner not found - warning, not fatal error
      // Deal will be created without assignment
      console.warn(`[ROW ${rowNumber}] Owner "${ownerValue}" not found.`);
      // ❌ assignedToId остается null
    }
  } else if (defaultAssignedToId) {
    // Если ownerField пустой для этой строки, используем дефолт
    assignedToId = defaultAssignedToId;
  }
}
```

### Проблемы:

1. **Если пользователь не найден → assignedToId = null** (должен быть fallback)
2. **Нет fallback на userId** (тот кто импортирует)

### Правильная логика:

```typescript
if (ownerField) {
  const ownerValue = getValue(ownerField);
  if (ownerValue) {
    // Пытаемся найти пользователя
    if (foundUserId) {
      assignedToId = foundUserId;
    } else {
      // Пользователь не найден - используем fallback
      assignedToId = defaultAssignedToId || userId; // ✅ Fallback
      // Можно также добавить warning для пользователя
    }
  } else {
    // ownerField пустой - используем дефолт
    assignedToId = defaultAssignedToId || userId; // ✅ Fallback
  }
} else {
  // Нет маппинга ownerField - используем дефолт
  assignedToId = defaultAssignedToId || userId; // ✅ Fallback
}
```

---

## 🔍 Проблема 3: rejectionReasons не сохраняются

### Анализ схемы Prisma:

```prisma
model Deal {
  rejectionReasons  String[]  @default([])
}
```

**rejectionReasons** - это просто массив строк (`String[]`), НЕ custom field.

### Анализ фронтенда:

В `deal-detail.tsx` используется Select с опциями из переводов:
```typescript
// В переводах есть:
"rejectionReason": {
  "price": "Price",
  "competitor": "Competitor",
  "timing": "Timing",
  "budget": "Budget",
  "requirements": "Requirements",
  "other": "Other"
}
```

НО это только для отображения на фронтенде - в БД rejectionReasons это просто String[].

### Текущая логика:

```typescript
// mapDealRow:
const rejectionReasons: string[] = [];
if (mapping.rejectionReasons) {
  const reasonsValue = getValue(mapping.rejectionReasons);
  if (reasonsValue) {
    rejectionReasons.push(...reasonsValue.split(',').map((r) => r.trim()).filter(Boolean));
  }
}
// return: rejectionReasons: rejectionReasons.length > 0 ? rejectionReasons : undefined

// batchCreateDeals:
rejectionReasons: row.rejectionReasons || [],
```

### Возможные проблемы:

1. **Данные не извлекаются** - нужно проверить логи
2. **Данные извлекаются, но не передаются** - нужно проверить структуру данных
3. **Данные передаются, но не сохраняются в Prisma** - нужно проверить Prisma запрос

### Требования пользователя:

> "Причина отказа это поле набираемый список. Логика такая что если в нашем списке нет значений как в таблице он должен дополнить список значениями которые есть в csv. Такая логика должна быть для всех полей с набираемым списком"

**Это означает:**
1. rejectionReasons - это поле с набираемым списком (multi-select)
2. Где-то есть список допустимых значений (опций)
3. Если значения из CSV отсутствуют в списке → добавить их в список
4. Затем сохранить rejectionReasons со всеми значениями

**НО:** В Prisma схеме rejectionReasons это просто String[] - нет связи с CustomField или отдельной таблицей опций.

**Вопросы:**
1. Где хранятся опции для rejectionReasons? 
   - Возможно в конфигурации системы?
   - Возможно в отдельной таблице/настройках?
   - Или это просто массив строк без предопределенных значений?

2. Если это просто массив строк без ограничений, то проблема может быть в том, что данные просто не сохраняются.

3. Пользователь говорит "набираемый список" - возможно это означает, что на фронтенде есть список опций, который нужно дополнить новыми значениями из CSV.

### Гипотезы:

1. **rejectionReasons хранится как String[], но есть список предопределенных значений** (например, в конфигурации или переводах)
2. **Нужно:**
   - Найти где хранятся опции rejectionReasons (возможно в CustomField или в настройках)
   - Если значений из CSV нет в опциях → добавить их в опции
   - Сохранить rejectionReasons со всеми значениями

3. **Или:** rejectionReasons это обычное поле, и проблема в том, что данные просто не доходят до БД (нужно проверить логи)

---

## 🔍 Проблема 4: Поля сделки не сохраняются (amount, budget, description и т.д.)

### Текущая логика:

```typescript
// mapDealRow возвращает:
return {
  amount: getValue(mapping.amount) || null,
  budget: getValue(mapping.budget) || null,
  description: sanitizeOptionalTextFields(getValue(mapping.description)) || null,
  // ...
};

// В batchCreateDeals:
const amountValue = row.amount !== undefined && row.amount !== null ? Number(row.amount) : null;
const budgetValue = row.budget !== undefined && row.budget !== null ? Number(row.budget) : null;

const baseDealData = {
  budget: budgetValue,
  description: row.description || null,
  // ...
};

// Для создания:
toCreate.push({
  ...baseDealData,
  amount: amountValue !== null && amountValue !== undefined ? amountValue : 0,
});
```

### Возможные проблемы:

1. **Данные не извлекаются из CSV** (проблема в mapDealRow)
2. **Данные не передаются в dealsWithNumber** (проблема в создании dealToCreate)
3. **Данные не сохраняются в Prisma** (проблема в batchCreateDeals)

### Нужно проверить логи:

1. `[MAP DEAL ROW RESULT]` - есть ли amount, budget, description?
2. `[IMPORT DEAL DATA]` - есть ли amount, budget, description?
3. `[BATCH CREATE DEAL DATA]` - есть ли amount, budget, description?
4. Prisma запрос - какие данные передаются?

---

## 📋 Резюме

### Проблема 1: Поля контакта не передаются
**Причина:** Неправильный индекс CSV строки - после фильтрации validRows индексы не соответствуют исходным rows
**Решение:** Сохранять исходный индекс CSV строки во время обработки processedRows

### Проблема 2: assignedToId не работает
**Причина:** Нет fallback на defaultAssignedToId/userId когда пользователь не найден
**Решение:** Добавить fallback: assignedToId = defaultAssignedToId || userId

### Проблема 3: rejectionReasons не сохраняются
**Вопрос:** Где хранятся опции для rejectionReasons?
- Если это просто String[] без ограничений → проблема может быть в том, что данные не сохраняются
- Если есть список опций → нужно найти где он хранится и дополнить его значениями из CSV

### Проблема 4: Поля сделки не сохраняются
**Причина:** Нужно проверить логи чтобы понять на каком этапе теряются данные



