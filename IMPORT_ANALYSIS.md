# Анализ проблем импорта сделок

## 🔍 Проблема 1: Поля контакта не передаются

### Текущая логика:
1. Во время обработки `validRows` сохраняются данные контактов в `dealContactDataMap`
2. После создания сделок, для каждой сделки создается контакт из `dealContactDataMap`

### Проблемы:

**1. Неправильный индекс CSV строки:**
```typescript
const csvRowIndex = rowIndex; // ❌ Это индекс в validRows, а не в rows
```

`validRows` - это отфильтрованные строки из `processedRows`, которые соответствуют `rows` по индексу, НО только для тех строк, которые прошли валидацию. Индекс `rowIndex` в `validRows` НЕ соответствует индексу в исходном массиве `rows`.

**2. Неправильный поиск processedRow:**
```typescript
const processedRow = processedRows.find(r => r.title === row.title && r.stageId === row.stageId);
```

Поиск по `title` и `stageId` может быть неточным, если есть дубликаты или если title/stageId изменились.

**3. Логика сохранения данных контактов слишком сложная:**
- Нужно найти соответствующий `processedRow`
- Потом найти индекс в `processedRows`
- Потом использовать этот индекс для доступа к `rows[csvRowIndex]`

### Правильное решение:
Нужно сохранять индекс исходной CSV строки (`rows`) во время обработки, а не пытаться его восстановить позже.

---

## 🔍 Проблема 2: assignedToId/ownerId не работает правильно

### Текущая логика:
```typescript
if (foundUserId) {
  assignedToId = foundUserId;
} else {
  // Owner not found - warning, not fatal error
  // Deal will be created without assignment
  console.warn(`[ROW ${rowNumber}] Owner "${ownerValue}" not found. Deal will be created without assignment.`);
}
```

### Проблемы:
1. Если пользователь не найден, `assignedToId` остается `null`
2. Нет fallback на `defaultAssignedToId` (тот кто импортирует)
3. Нет логики для предложения сопоставить вручную

### Требования:
1. Если пользователь не найден → использовать `defaultAssignedToId` (тот кто импортирует)
2. Если `defaultAssignedToId` не указан → использовать `userId` (тот кто импортирует)
3. В будущем: предложить сопоставить вручную (для этого нужно добавить warnings/errors с предложениями)

---

## 🔍 Проблема 3: rejectionReasons не сохраняются

### Текущая логика:
```typescript
// Парсинг rejectionReasons (разделенные запятой)
const rejectionReasons: string[] = [];
if (mapping.rejectionReasons) {
  const reasonsValue = getValue(mapping.rejectionReasons);
  if (reasonsValue) {
    rejectionReasons.push(...reasonsValue.split(',').map((r) => r.trim()).filter(Boolean));
  }
}

// В return:
rejectionReasons: rejectionReasons.length > 0 ? rejectionReasons : undefined,
```

Затем в `batchCreateDeals`:
```typescript
rejectionReasons: row.rejectionReasons || [],
```

### Проблемы:
1. Данные извлекаются и парсятся правильно
2. НО: rejectionReasons это поле с набираемым списком (как custom field с MULTI_SELECT)
3. Если в списке нет значений из CSV, они не будут сохранены
4. Нужно ДОПОЛНИТЬ список опций значениями из CSV, если их там нет

### Требования:
1. Перед сохранением rejectionReasons нужно проверить, какие значения есть в системе
2. Если значения из CSV отсутствуют в списке опций → добавить их в список опций
3. Затем сохранить rejectionReasons со всеми значениями (и старыми, и новыми)
4. Такая логика должна быть для ВСЕХ полей с набираемым списком (MULTI_SELECT, SELECT)

### Вопросы:
1. Где хранятся опции для rejectionReasons? Это системное поле или custom field?
2. Если это системное поле Deal - где хранятся его опции?
3. Если это custom field - нужно найти CustomField и обновить его options

---

## 🔍 Проблема 4: Поля сделки не сохраняются (amount, budget, description и т.д.)

### Возможные причины:
1. Данные не извлекаются из CSV (проблема в `mapDealRow`)
2. Данные извлекаются, но не передаются в `batchCreateDeals`
3. Данные передаются, но не сохраняются в Prisma

### Нужно проверить:
1. Логи `[MAP DEAL ROW RESULT]` - есть ли там данные?
2. Логи `[IMPORT DEAL DATA]` - есть ли там данные?
3. Логи `[BATCH CREATE DEAL DATA]` - есть ли там данные?
4. Prisma запрос - какие данные передаются в `create`/`update`?

---

## 📋 Резюме проблем

1. **Поля контакта не передаются:**
   - Неправильный индекс CSV строки при сохранении данных контактов
   - Нужно сохранять индекс исходной CSV строки во время обработки

2. **assignedToId не работает:**
   - Нет fallback на defaultAssignedToId или userId
   - Нужно добавить fallback логику

3. **rejectionReasons не сохраняются:**
   - Нужно дополнить список опций значениями из CSV
   - Нужно понять, где хранятся опции rejectionReasons
   - Нужно реализовать логику для всех полей с набираемым списком

4. **Поля сделки не сохраняются:**
   - Нужно проверить логи, чтобы понять на каком этапе теряются данные



