# Как посмотреть логи бэкенда в CodeSpace

## Способ 1: Запустить сервер в текущем терминале (рекомендуется)

1. Откройте новый терминал в CodeSpace (Terminal → New Terminal или `Ctrl+``)
2. Перейдите в директорию бэкенда:
   ```bash
   cd crm-backend
   ```
3. Остановите существующий процесс (если запущен):
   ```bash
   pkill -f "nest start"
   ```
4. Запустите сервер:
   ```bash
   npm run start:dev
   ```
5. Все логи будут выводиться в этот терминал
6. Ищите строки с префиксом `🔥` для диагностических логов

## Способ 2: Использовать готовый скрипт

```bash
cd crm-backend
./start-with-logs.sh
```

## Способ 3: Найти терминал, где уже запущен сервер

1. В CodeSpace откройте все вкладки терминалов
2. Найдите вкладку, где запущен `nest start --watch`
3. Там будут все логи

## Способ 4: Перенаправить логи в файл

```bash
cd crm-backend
npm run start:dev > backend.log 2>&1 &
tail -f backend.log
```

## Важные логи для отладки импорта:

- `🔥🔥🔥 CONTROLLER ENTRY` - вход в контроллер importDeals
- `🔥 PipelinesController.findAll` - получение pipelines
- `[IMPORT META]` - получение метаданных импорта
- `[PIPELINES SERVICE]` - работа с pipelines
- `🔥 VALIDATION ERROR` - ошибки валидации
- `🔥 EXCEPTION FILTER` - ошибки в exception filter
- `🔥 GLOBAL ERROR` - глобальные ошибки

## Если не видите логи:

1. Убедитесь, что сервер запущен: `ps aux | grep nest`
2. Проверьте порт: `lsof -ti:3001`
3. Перезапустите сервер: `cd crm-backend && npm run start:dev`




