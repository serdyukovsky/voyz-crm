# Анализ производительности открытия карточки сделки

## Вывод: ДА, карточка открывается медленно

При открытии карточки сделки выполняется **множество запросов**, часть из которых неэффективны.

## Что происходит при открытии карточки

### 1. Backend: `GET /api/deals/${id}`

**Файл:** `crm-backend/src/deals/deals.service.ts:395` - метод `findOne()`

**Выполняется:**
1. Основной запрос сделки через `findUnique` с `select` (✅ уже оптимизировано)
2. Вызывается `formatDealResponse(deal)` который делает:

#### Проблема #1: Загрузка customFields (строка ~655)

```typescript
const customFields = allCustomFields || await this.customFieldsService.findByEntity('deal');
```

- ❌ **Выполняется КАЖДЫЙ раз** при открытии карточки
- ❌ Это отдельный запрос к БД: `SELECT * FROM custom_fields WHERE entityType = 'deal' AND isActive = true`
- ⏱️ Время: ~50-100ms
- 💡 **Решение:** Кешировать customFields (они меняются редко)

#### Проблема #2: getContactStats() (строка ~619)

```typescript
const contactStats = contactStatsMap?.get(deal.contact.id) || 
  (contactStatsMap ? null : await this.getContactStats(deal.contact.id));
```

**Метод getContactStats() (строка ~991):**
```typescript
private async getContactStats(contactId: string) {
  const deals = await this.prisma.deal.findMany({
    where: { contactId },
    select: { id: true, amount: true, closedAt: true },
  });
  // Затем фильтрация и подсчет в памяти
}
```

- ❌ **Загружает ВСЕ сделки контакта** через `findMany`
- ❌ Если у контакта 1000 сделок → загружается 1000 записей
- ❌ Затем фильтрация и подсчет в JavaScript (не на уровне БД)
- ⏱️ Время: **200-500ms** при 100 сделок, **1-3 секунды** при 1000+ сделок
- 💡 **Решение:** Использовать `aggregate` с `_count` и `_sum` на уровне БД

#### Проблема #3: getCompanyStats() (строка ~675)

Аналогично getContactStats - загружает все сделки компании через `findMany`.

- ❌ Та же проблема
- ⏱️ Время: аналогично contact stats
- 💡 **Решение:** Использовать `aggregate`

### 2. Frontend: Дополнительные хуки

**Файл:** `CRM/components/crm/deal-detail.tsx:164-167`

При открытии карточки выполняются **4 дополнительных запроса**:

1. **useDealTasks** → `GET /api/tasks?dealId=${dealId}`
2. **useDealActivity** → (mock данные, не критично)
3. **useActivity** → `GET /api/activities?entityType=deal&entityId=${dealId}`
4. **useDealFiles** → (mock данные, не критично)

Эти запросы выполняются параллельно, но **блокируют** отображение карточки (loading state).

### 3. Дополнительные загрузки на фронтенде

**Файл:** `CRM/components/crm/deal-detail.tsx:169-195`

- `getContacts()` - загрузка всех контактов для селектора
- `getUsers()` - загрузка всех пользователей для селектора

Эти запросы выполняются при каждом открытии карточки, даже если данные уже загружены.

## Оценка времени загрузки

**Сценарий 1: Контакт с 10 сделками**
- findOne: ~100ms
- customFields: ~50ms
- getContactStats: ~100ms
- getCompanyStats: ~100ms (если есть компания)
- tasks: ~100ms
- activities: ~100ms
- contacts: ~150ms
- users: ~100ms
- **Итого: ~700-800ms**

**Сценарий 2: Контакт с 500 сделками** (реальный кейс)
- findOne: ~100ms
- customFields: ~50ms
- getContactStats: ~500-1000ms ❌
- getCompanyStats: ~500-1000ms ❌
- tasks: ~100ms
- activities: ~100ms
- contacts: ~150ms
- users: ~100ms
- **Итого: ~1.5-2.5 секунды** ⚠️

**Сценарий 3: Контакт с 2000+ сделками**
- getContactStats: ~2-5 секунд ❌❌
- getCompanyStats: ~2-5 секунд ❌❌
- **Итого: 4-10 секунд** 🔴

## Выявленные проблемы

### 🔴 КРИТИЧНО: Неэффективные stats запросы

**Проблема:** `getContactStats()` и `getCompanyStats()` используют `findMany` + фильтрацию в памяти

**Код:**
```typescript
// Текущая реализация (МЕДЛЕННО)
const deals = await this.prisma.deal.findMany({
  where: { contactId },
  select: { id: true, amount: true, closedAt: true },
});
const activeDeals = deals.filter((d) => !d.closedAt);
const closedDeals = deals.filter((d) => d.closedAt);
const totalDealVolume = closedDeals.reduce((sum, deal) => sum + Number(deal.amount), 0);
```

**Проблемы:**
- Загружает ВСЕ записи в память
- Фильтрация в JavaScript вместо SQL
- Передача большого объема данных по сети

**Оптимальная реализация:**
```typescript
// С использованием aggregate (БЫСТРО)
const stats = await this.prisma.deal.aggregate({
  where: { contactId },
  _count: { id: true },
  _sum: { amount: true },
});

const closedStats = await this.prisma.deal.aggregate({
  where: { contactId, closedAt: { not: null } },
  _count: { id: true },
  _sum: { amount: true },
});
```

**Ускорение:** 10-100x (зависит от количества сделок)

### 🟡 ВЫСОКИЙ: Загрузка customFields каждый раз

**Проблема:** CustomFields загружаются при каждом открытии карточки

**Решение:** Кеширование (memory cache или Redis)

**Ускорение:** -50-100ms

### 🟡 СРЕДНИЙ: Множественные параллельные запросы

**Проблема:** Все запросы выполняются одновременно, но UI блокируется до полной загрузки

**Решение:** Progressive loading - показывать основную информацию сразу

**Эффект:** Воспринимаемое время загрузки -70%

### 🟢 НИЗКИЙ: Загрузка contacts и users каждый раз

**Проблема:** Списки контактов и пользователей загружаются при каждом открытии

**Решение:** Глобальное кеширование или загрузка только при необходимости

## Рекомендации по оптимизации

### Приоритет 1 (критично, быстрый эффект):

1. **Оптимизировать getContactStats и getCompanyStats**
   - Заменить `findMany` + фильтрация на `aggregate`
   - Использовать 2 запроса: общий count и closed deals count
   - **Эффект: 10-100x быстрее** (особенно для контактов с большим количеством сделок)
   - **Риск:** Низкий (только изменение реализации)

2. **Кешировать customFields**
   - Кешировать в памяти на 5-10 минут
   - Инвалидировать при изменении
   - **Эффект: -50-100ms**

### Приоритет 2 (средний эффект):

3. **Progressive loading на фронтенде**
   - Показывать основную информацию сразу после findOne
   - Подгружать tasks, activities, files асинхронно
   - **Эффект:** Воспринимаемое время загрузки -70%

4. **Кеширование findOne**
   - Кешировать на 1-2 минуты с инвалидацией при изменениях
   - **Эффект:** Повторные открытия мгновенные

## Итоговая оценка

**Текущая производительность:**
- Маленький контакт (10 сделок): ~700-800ms ✅ Приемлемо
- Средний контакт (100 сделок): ~1-1.5 секунды ⚠️ Заметно
- Большой контакт (500+ сделок): **2-5 секунд** ❌ Медленно
- Очень большой (2000+ сделок): **5-10 секунд** 🔴 Очень медленно

**После оптимизации stats запросов:**
- Любой контакт: **~300-500ms** ✅ Быстро
- Большой контакт: **~500-700ms** ✅ Приемлемо

## Вывод

**ДА, карточка действительно открывается медленно**, особенно для контактов/компаний с большим количеством сделок.

**Основная проблема:** Неэффективные stats запросы через `findMany` вместо `aggregate`.

**Рекомендация:** Начать с оптимизации `getContactStats()` и `getCompanyStats()` - это даст наибольший эффект (10-100x улучшение).
