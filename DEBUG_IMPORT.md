# 🔥 КАК ПРОВЕРИТЬ ЧТО ИМПОРТ РАБОТАЕТ

## ✅ ШАГ 1: Проверить что бэкенд работает

```bash
curl http://localhost:3001/api/health
```

Должен вернуть что-то (не ошибку).

---

## ✅ ШАГ 2: Проверить в БРАУЗЕРЕ (Network Tab)

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Запустите импорт
4. Найдите запрос `POST /api/import/deals`
5. Проверьте:
   - **Status:** должен быть 200 (или ошибка, но запрос должен быть)
   - **Payload:** что отправляется на бэкенд

**Если запроса НЕТ** → проблема на фронтенде (импорт не запускается)

**Если запрос ЕСТЬ** → смотрите ответ от бэкенда

---

## ✅ ШАГ 3: Добавить логи прямо в код (если ничего не работает)

Если логи не появляются, добавьте их прямо в код:

Откройте файл:
`crm-backend/src/import-export/import-export.controller.ts`

Найдите метод `importDeals` и в самом начале добавьте:

```typescript
async importDeals(...) {
  console.log('🔥🔥🔥 ИМПОРТ ЗАПУЩЕН!!!');
  console.log('🔥🔥🔥 Тело запроса:', JSON.stringify(dto, null, 2));
  // ... остальной код
}
```

Затем перезапустите бэкенд и запустите импорт.

---

## ✅ ШАГ 4: Проверить что бэкенд перезагрузился

После изменения кода бэкенд должен автоматически перезагрузиться (если в watch режиме).

Или перезапустите вручную:

```bash
cd /workspaces/voyz-crm/voyz-crm/crm-backend
pkill -f "nest start"
npm run start:dev
```

---

## 🎯 БЫСТРАЯ ПРОВЕРКА

1. **Откройте браузер → DevTools → Network**
2. **Запустите импорт**
3. **Смотрите есть ли запрос к `/api/import/deals`**
4. **Если запроса нет → проблема на фронтенде**
5. **Если запрос есть → проблема в бэкенде или логи не пишутся**



