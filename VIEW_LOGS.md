# Как посмотреть логи бэкенда в CodeSpace

## Способ 1: Терминал CodeSpace (рекомендуется)

1. Откройте терминал в CodeSpace (Terminal → New Terminal)
2. Перейдите в директорию бэкенда:
   ```bash
   cd crm-backend
   ```
3. Запустите сервер в режиме разработки:
   ```bash
   npm run start:dev
   ```
4. Все логи будут выводиться в этот терминал
5. Ищите строки с префиксом `🔥` для диагностических логов

## Способ 2: Проверка через curl

Выполните запрос и посмотрите ответ:
```bash
cd crm-backend
curl -X POST http://localhost:3001/api/import/deals?dryRun=true \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"rows":[{"test":"value"}],"mapping":{"title":"test"},"pipelineId":"test-id"}'
```

## Способ 3: Добавить endpoint для просмотра логов

Можно добавить временный endpoint для просмотра последних логов (только для разработки).

## Важные логи для отладки импорта:

- `🔥🔥🔥 CONTROLLER ENTRY` - вход в контроллер
- `🔥 DTO value` - содержимое запроса
- `🔥 VALIDATION ERROR` - ошибки валидации
- `🔥 EXCEPTION FILTER` - ошибки в exception filter
- `🔥 GLOBAL ERROR` - глобальные ошибки

## Если не видите логи:

1. Убедитесь, что сервер запущен: `ps aux | grep nest`
2. Проверьте порт: `lsof -ti:3001`
3. Перезапустите сервер: `cd crm-backend && npm run start:dev`




